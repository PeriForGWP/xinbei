<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.czd.xinbei" >

    <uses-permission android:name="ANDROID.PERMISSION.INTERNET" />
    <uses-permission android:name="ANDROID.PERMISSION.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".avtivity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".avtivity.SearchActivity"
            android:label="@string/title_activity_search"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".avtivity.InformationActivity"
            android:label="@string/title_activity_information"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".avtivity.TVActivity"
            android:label="@string/title_activity_tv" >
        </activity>
        <activity android:name=".avtivity.ProjectActivity" />
        <activity
            android:name=".avtivity.ProblemActivity"
            android:label="@string/title_activity_problem" >
        </activity>
        <activity
            android:name=".avtivity.AnswerActivity"
            android:label="@string/title_activity_answer" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
